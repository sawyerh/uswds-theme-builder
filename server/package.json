{
  "name": "server",
  "private": true,
  "scripts": {
    "deploy": "gcloud beta run deploy dart-sass-compiler --source .",
    "deploy:dev": "gcloud beta run deploy dart-sass-compiler-dev --source .",
    "postinstall": "./scripts/postinstall-uswds.sh"
  },
  "dependencies": {
    "uswds": "^2.12.0"
  }
}
