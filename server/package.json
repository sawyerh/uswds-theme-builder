{
  "name": "server",
  "private": true,
  "scripts": {
    "deploy": "gcloud beta run deploy dart-sass-compiler --source .",
    "dev": "ORIGIN=http://localhost:3000 dart run bin/server.dart",
    "postinstall": "./scripts/postinstall-uswds.sh"
  },
  "dependencies": {
    "uswds": "^2.12.0"
  }
}
